name: 'RCE Niet-Registergegevens-Monumenten ETL' 

env:
  BASE_URI: 'https://linkeddata.cultureelerfgoed.nl/'
  SRC_URI: 'https://kennis.cultureelerfgoed.nl/api.php'
  ARTIFACT_PATH: 'artifact-data.jsonld'
  GRAPH_ID: 'kennisbank-extract-latest-graph'
  DATASET_NAME: 'Niet-Registergegevens-Monumenten'
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  schedule:
  - cron: '0 15 * * *'
    timezone: 'Europe/Amsterdam' 

jobs:
  get-latest-extract:
    runs-on: ubuntu-latest 
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Get latest extract 
      run: |
        python src/extract_monumenten_service.py
      env:
        SRC_URI: ${{ env.SRC_URI }}
        TARGET_FILEPATH: ${{ env.ARTIFACT_PATH }}
        GRAPH_ID: ${{ env.GRAPH_ID }}   
    - name: Upload data artifact
      uses: actions/upload-artifact@v4
      with:
        name: data-artifact
        path: ${{ env.ARTIFACT_PATH }}
        overwrite: true

  push-to-triply:
    runs-on: ubuntu-latest
    needs: get-latest-extract
    steps:
    - name: Download data artifact
      uses: actions/download-artifact@v4
      with:
          name: data-artifact
    - name: Install dependencies
      run: |
        wget 'https://static.triply.cc/cli/triplydb-linux'
        chmod +x triplydb-linux
    - name: Push changes to TriplyDB API via CLI
      run: |
        ./triplydb-linux import-from-file --dataset "${{ env.DATASET_NAME }}" --token "${{ secrets.LDV_API_TOKEN }}" --account "rce" --url "https://api.linkeddata.cultureelerfgoed.nl" --default-graph-name "https://linkeddata.cultureelerfgoed.nl/rce/${{ env.DATASET_NAME }}/graphs/${{ env.GRAPH_ID }}" --mode "overwrite" ${{ env.ARTIFACT_PATH }} 

  sync-triply:
    runs-on: ubuntu-latest
    needs: push-to-triply
    steps:
    - name: Push changes to TriplyDB API via cURL
      run:  |
        curl -H 'Authorization: Bearer ${{ secrets.LDV_API_TOKEN }}' -H 'Content-Type: application/json' -X POST https://api.linkeddata.cultureelerfgoed.nl/datasets/rce/${{ env.DATASET_NAME }}/services/Niet-Registergegevens-Monumenten -d '{"sync": "true"}'
